-- -----------------------------------
-- メッセージログ管理 (Message Logs Management)
-- version 1.0
-- -----------------------------------

-- MessageLogs
CREATE TABLE message_logs (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT 'メッセージログのユニークなID',
    message_code VARCHAR(64) NOT NULL UNIQUE COMMENT 'メッセージコード（ユニーク）',
    site_id INT NOT NULL COMMENT '関連するサイトID',
    operator_id INT NOT NULL COMMENT 'メッセージを送信したオペレータID',
    user_id INT NOT NULL COMMENT 'メッセージを受信したユーザーID',
    message_type VARCHAR(32) NOT NULL COMMENT 'メッセージの種類（例: LINE, EMAIL）',
    message_content TEXT NOT NULL COMMENT 'メッセージ内容',
    sent_at DATETIME NOT NULL COMMENT 'メッセージ送信日時',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '作成日時',
    deleted_at DATETIME DEFAULT NULL COMMENT '削除日時（論理削除用）',
    FOREIGN KEY (site_id) REFERENCES sites(id) ON DELETE CASCADE,
    FOREIGN KEY (operator_id) REFERENCES operators(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) COMMENT='メッセージログ情報を管理するテーブル';
